# ───── Java / Maven build artifacts ───────────────────────────
target/                       # Maven’s build output
*.class                       # Compiled bytecode
dependency-reduced-pom.xml    # Shade plugin temp file
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
buildNumber.properties
.mvn/timestamps.properties
.mvn/wrapper/maven-wrapper.jar

# ───── IDE & editor folders ───────────────────────────────────
# IntelliJ / Android Studio
.idea/
*.iml
*.iws
out/

# Eclipse
.project
.classpath
.settings/

# VS Code
.vscode/

# NetBeans
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

# ───── OS-specific junk ───────────────────────────────────────
.DS_Store          # macOS Finder
Thumbs.db          # Windows image cache
ehthumbs.db
Icon?              # resource fork files
._*                # macOS extra files

# ───── Logs & runtime files ───────────────────────────────────
*.log
hs_err_pid*        # JVM crash logs
replay_pid*        # JRebel / HotSwap

# ───── Misc tools ─────────────────────────────────────────────
# Spotless, Checkstyle temp
spotless.*.backup

# Coverage (JaCoCo) — keep even if not used yet; harmless
jacoco*.exec
